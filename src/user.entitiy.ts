import { Column, Entity, PrimaryGeneratedColumn } from 'typeorm';

//15분부터 보기
@Entity('users')
export class User {
  @PrimaryGeneratedColumn()
  id: number;

  @Column()
  name: string;

  @Column({ unique: true })
  email: string;

  @Column()
  password: string;
}
